Generate a detailed JIRA bug report using the test case details:

Test Case Details:
Title: {title}
Failing Step: {step_content}
All Steps: {all_steps}
Expected Result: {expected}
Test Case ID: {case_id}
Device: {platform}
ASIN: {file_path}

Structure your JIRA issue as follows:

Bug Title: [Read the Expected Result carefully to understand if this is a POSITIVE or NEGATIVE test case.

IMPORTANT: If the Expected Result says something "should NOT happen" or "shouldn't be created/allowed", then the BUG is when that thing DOES happen.

Required format: "[Feature Name] Bug description"
Rules: NO hyphen between the brackets and the bug description. Example: "[TOC Navigation] TOC not enabled after selecting hamburger menu" not "[TOC Navigation] - TOC not enabled"
DO NOT include "[CRITICAL]" or any tag or prefix outside the feature brackets.

Rules for creating the title:
1. Extract the EXACT feature/component name from the test case steps or title
2. READ THE EXPECTED RESULT CAREFULLY:
   - If expected says "should NOT" or "shouldn't" → The bug is when it DOES happen
   - If expected says "should" → The bug is when it DOESN'T happen
3. The title must describe THE BUG (what's wrong), not the correct behavior
4. Compare actual vs expected behavior when relevant

Examples for NEGATIVE test cases (expected behavior is something should NOT happen):
- Step: "Try to create white space inside table viewer"
- Expected: "White space shouldn't be created inside table viewer"
- CORRECT TITLE: "Table Viewer - White space can be created inside table viewer" (describes the bug)
- WRONG TITLE: "Table Viewer - Unable to create white space inside table viewer" (describes correct behavior)

Examples for POSITIVE test cases (expected behavior is something should happen):
- Step: "Open Pdoc and do highlight on page"
- Expected: "Highlighted colors should same as LR books"
- CORRECT TITLE: "Highlight Feature - Highlighted colors not as bright as LR books" (describes the bug)

The title must describe THE ACTUAL BUG, not the correct/expected behavior.]

*Caught by:* Testcase execution
*Found in ASIN:* {file_path}
*Repro rate:* [Filled by tester]
*Issue found in Platform:* {platform}
*Device Specific (Yes/No):* [Filled by tester]
*Issue specific to Capability:* [Filled by tester]
*Test Account:* [Filled by tester]
*Related Testcase:* {testrail_url}

*Build & Device used:*
[Filled by tester]

*Steps to Reproduce:*
{all_steps}

*Actual Result:* [YOU MUST generate this as a real sentence based on the step and expected result. Do NOT leave as a placeholder. If expected says "should X" then write "X does not happen". If expected says "should NOT X" then write "X happens unexpectedly".]

*Expected Result:* [State what should happen — based on the Expected Result field above]

*Attachments:* [Filled by tester]

Guidelines:
• READ EXPECTED RESULT CAREFULLY - understand if it's a positive or negative test case
• Title must describe THE BUG (what's wrong), never the correct behavior
• Title must NOT include [CRITICAL] or any prefix tag
• For "should NOT" cases, the bug is when it DOES happen
• For "should" cases, the bug is when it DOESN'T happen
• Steps to Reproduce: copy {all_steps} exactly, include all steps from the test case
• Actual Result MUST be a real generated sentence — never a placeholder or bracket text
• Expected Result must be a single concise line from the test case expected result
• Use clear, technical language
• Be precise about what's wrong, not vague
• CRITICAL REMINDER: Always fill Actual Result with a real sentence. This is mandatory.